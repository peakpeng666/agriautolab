# 转折点：从「选择自然算法」到「证据纪律下生成算法」

日期：2026-08-24。本文是项目第三次方向转折的章程，批判性核验、
序列计划与非目标都在这里。前两次转折见 AUDIT_NOTE（VBS 差距 ~3% 否定
「更好的平均算法」；Fields2Benchmark 存在否定「再建基准平台」）。

## 一、转折定义

**AgriAutoLab = 农业 CPP 的研究仪器 + 分析/选择层**（定位不变），
其上发起生成式研究线：

> **把 LLM 驱动的自动启发式设计（AHD/AHSD，EoH/EoH-S 范式）引入农业 CPP：
> 在冻结的三目标 Pareto 基准上进化覆盖规划启发式（行向选择 / 条带排序 /
> 地头转弯选择），适应度 = 前沿超体积增量，全部运行走既有证据链。**
> 不可主张「首次进化互补启发式集合」（EoH-S 已做）；可主张的是领域引入
> + 农业语义三目标 ΔHV 评价信号 + 冻结真实地块基准与证据链验证。

学术主张随之从「选得更好」升级为可检验的生成式问题：
自然算法池互补不足时，自动算法设计能否真实增加 Pareto 超体积？
——这正是 L4 选择层反衬出来的下一问。

## 二、外部输入的批判性核验（两份 AI 报告）

| 断言 | 核验结果 |
|---|---|
| 工程验收报告：main=76e4a92、546 passed、冻结件未动、双自检绿 | **本地复跑全部属实**（PR #7 仅 2 文件，临时验收 PR #6 已关） |
| `validate_f2c_recorded.py --self-check` 此前坏（5 列旧 CSV vs 13 列 schema），已修 | 属实；有效教训：**单测绿 ≠ 公开入口可运行**，已补 subprocess 回归 |
| O2 重跑：路线身份对齐后 transit 中位差 −0.124%，skip_one +55.7% | 机制属实；**非新知识**——ITERATION_REPORT 早已记录「配对后残差 −0.14%」，此为独立再确认 + 路线策略量化。旧开放项可关闭 |
| P0：H2 修正案 03 口径统计退化 | **实证成立且更糟**：特征=设计偏移+主轴估计噪声（±0.03 rad），田级中位全挤 π/4±0.03——披着数据外衣的仪器噪声。修正案 04 已立（田内重复测量设计） |
| P0：H3 端点不随偏好变化 | 成立；修正案 04 改为偏好条件 Tchebycheff 悔值，修正案 05 最终冻结为 `PREFERENCE_GRID_V1` 22 点 |
| P1/P2：12 特征文档、530→546、examples、CLI | 属实：12 特征与 546 已修（PR #8）；examples/ 已补齐（D0.4）；CLI 明确延后 |
| 「停止大规模重构」 | 同意——与本仓此前异议一致，L1–L3 冻结 |
| 调研报告：LLM 进化 × 农业 CPP 是空白 | **联网抽查成立**（EoH/EoH-S/G-LNS/LLM4AD 文献簇与农业 CPP 簇未交叉）；论文定稿前须再次核查 |
| 调研机会排序（进化最可行） | 同意：评估器（本基准）与演化基建（Agent 层四闸）现成，唯一缺真模型后端 |

## 三、序列计划（顺序不可换；confirmatory protocol 已冻结）

0. **D0.4 封口（已完成）**：修正案 05——偏好网格坐标级冻结
   （PREFERENCE_GRID_V1，22 点，哈希进协议）、零权重契约、H2 常数响应
   记 0、H3 田级聚合与非 oracle 基线、不可行罚则、检验统计量总表。
   **此后 confirmatory protocol 冻结，只允许新 study 注册。**
1. **D1 CV 折身份（已完成）**：从 v7 manifest 的逐田 `licenses` 恢复完整
   235-field universe，排除已封存 70-field holdout 后，对训练侧 165 块田按
   `SHA256(seed=20260822, field_id)` 稳定伪随机排序并 round-robin 分 10 折；
   折大小 = 17×5 + 16×5。完整 field→fold 表落 `evidence/v7/cv_assignment.json`，
   `assignment_hash=bcf237a782e9…4113`，`spec_hash=dc0be0812b31…4cf7`，并作为
   `evidence/block_d/ledger.jsonl` 的 genesis。**禁止训练代码重新 split。**
   首轮实现曾错误把 `effective_pool_size_by_instance` 当 field universe，干净
   runner 立即发现 12 个 holdout field 缺席；已改为结果无关的 `licenses` 表并
   加回归测试。这个失败同时构成 D2 的重要警示：结果摘要不能定义样本全集。
2. **D2**：拒绝/有效池普查，明确 nominal / static-applicable / validator-OK
   三层集合，生成训练前可知的 applicability 真值与不可行结构报告。
3. **D3–D4**：选择评估协议（三层基线）+ 推荐器（sklearn 式
   fit/recommend）。**H1/H2 只依赖冻结语料，D2 后即可与推荐器开发并行执行。**
4. **D5–D7**：H1 → H2 → H3（**留出集只在此一次性打开**，双轨 70/68）。
   这一步同时产出「自然池互补有限」的量化证据——生成式研究线的动机。
5. **D8**：探索性 ISA / 解释 / 消融（明确标注 exploratory）。
6. **Study-002 预注册（生成式）**：个体 = 启发式代码（三槽位）；
   适应度 = 训练折上 ΔHV（对当前前沿）；留出集只在最终评估用一次；
   对照 = 自然池 13 配置、随机启发式、**EoH-S（AAAI 2026，互补启发式
   集合进化——本研究线最近的方法学邻居，必须作直接基线而非仅
   related work）**；LLM 后端版本/温度/种子/成本全部进证据链；四道
   安全闸与对抗式复核既有实现直接复用。
7. **可选扩展（Study-003+ 候选，不捆绑）**：能耗第四目标（USV 功率-
   边权方法移植）、课程 RL、扩散轨迹生成、多机 MAPF。

## 四、非目标（明确不做）

- 不再大规模改包名/搬目录（canonical+wire 双层已定）；
- 不在 Block D 结论出来之前动 LLM 线的任何实跑；
- 不把能耗/多机等扩展捆进本轮；
- 留出集与预注册纪律不因新方向松动（探针田披露与双轨并报沿用）。

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| LLM 非确定性破坏可复现性 | 请求-响应全量落盘进 evidence；模型版本钉死；重放验证 |
| 进化出的启发式过拟合训练折 | 适应度只看训练折，留出集一次性评估（与 H3 同构的纪律） |
| 「空白」判断过期 | 定稿前重跑聚焦文献核查 |
| 生成线抢走选择线的统计严谨性 | 序列计划锁定：Block D 先行，Study-002 后注册后跑 |
